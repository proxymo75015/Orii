<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions nécessaires pour le Bluetooth et la localisation -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Suppression de la permission INTERNET, l'app étant autonome -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->
    <!-- Permission pour poster des notifications (à conserver si l'app envoie des notifications locales) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Fonctionnalités Bluetooth requises -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Déclaration du service principal de l'application -->
        <service
            android:name=".services.AppService"
            android:exported="false" />

        <!-- MainActivity lancée au démarrage -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
